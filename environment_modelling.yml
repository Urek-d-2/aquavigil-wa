name: aquavigil-modelling
channels:
  - conda-forge
  - defaults

dependencies:
  - python>=3.9
  - pandas>=2.0
  - numpy>=1.24
  - scipy>=1.11
  - statsmodels>=0.14
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - seaborn>=0.12
  - jupyter>=1.0
  - jupyterlab>=4.0

  # --- Tests ---
  - pytest>=7.4
  - pytest-cov>=4.1

  - pip:
    - pmdarima>=2.0.4       # auto_arima pour sélection ARIMA(p,d,q)
    - fastapi>=0.104        # Brique 3 — backend dashboard
    - uvicorn>=0.24         # Serveur ASGI FastAPI
